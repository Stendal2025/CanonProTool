version: "3.8"

services:
  canon-pro-tool:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    environment:
      - STREAMLIT_SERVER_MAX_UPLOAD_SIZE=10
      - STREAMLIT_SERVER_RUN_ON_SAVE=false
    restart: unless-stopped
